# L2W
L2W maps familiar Linux commands to their Windows equivalents. You enter a Linux command, and the script executes the corresponding Windows command. Arguments (flags) correspond to Windows syntax — full support for Linux flags is not implemented, as this would require a full-fledged emulator. This tool is intended for entertainment purposes only.
